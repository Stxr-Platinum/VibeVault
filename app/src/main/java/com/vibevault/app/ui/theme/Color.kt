package com.vibevault.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color.kt — Stitch "Music Streaming Design System" tokens.
 * Source: Stitch Project 6665642734514469828, Asset 19171ab819a8434a9af96e9b735f60d7
 *
 * All hex values are fetched directly from the Stitch namedColors object.
 */

// ── Surface Hierarchy (deep charcoal → highlight) ──────────
val VibeBg                    = Color(0xFF0E150E)
val VibeSurfaceDim            = Color(0xFF0E150E)
val VibeSurfaceContainerLowest = Color(0xFF091009)
val VibeSurfaceContainerLow   = Color(0xFF161D16)
val VibeSurface               = Color(0xFF1A211A)   // surface_container
val VibeSurfaceHigh           = Color(0xFF242C24)   // surface_container_high
val VibeSurfaceHighest        = Color(0xFF2F372E)   // surface_container_highest
val VibeSurfaceBright         = Color(0xFF333B33)
val VibeSurfaceVariant        = Color(0xFF2F372E)

// ── Primary (Spotify Green) ────────────────────────────────
val VibePrimary               = Color(0xFF53E076)
val VibeOnPrimary             = Color(0xFF003914)
val VibePrimaryContainer      = Color(0xFF1DB954)
val VibeOnPrimaryContainer    = Color(0xFF004118)
val VibeInversePrimary        = Color(0xFF006E2D)

// ── Secondary ──────────────────────────────────────────────
val VibeSecondary             = Color(0xFF99D59D)
val VibeOnSecondary           = Color(0xFF003914)
val VibeSecondaryContainer    = Color(0xFF1C5329)
val VibeOnSecondaryContainer  = Color(0xFF8CC690)

// ── Tertiary (warm accent) ─────────────────────────────────
val VibeTertiary              = Color(0xFFFFB3B3)
val VibeOnTertiary            = Color(0xFF680114)
val VibeTertiaryContainer     = Color(0xFFFF767B)
val VibeOnTertiaryContainer   = Color(0xFF730A1B)

// ── Error ──────────────────────────────────────────────────
val VibeError                 = Color(0xFFFFB4AB)
val VibeOnError               = Color(0xFF690005)
val VibeErrorContainer        = Color(0xFF93000A)
val VibeOnErrorContainer      = Color(0xFFFFDAD6)

// ── Text & Content ─────────────────────────────────────────
val VibeOnSurface             = Color(0xFFDDE5D9)
val VibeOnSurfaceVariant      = Color(0xFFBCCBB9)
val VibeOnBackground          = Color(0xFFDDE5D9)
val VibeOutline               = Color(0xFF869585)
val VibeOutlineVariant        = Color(0xFF3D4A3D)

// ── Inverse ────────────────────────────────────────────────
val VibeInverseSurface        = Color(0xFFDDE5D9)
val VibeInverseOnSurface      = Color(0xFF2B322A)

// ── Surface Tint ───────────────────────────────────────────
val VibeSurfaceTint           = Color(0xFF53E076)

// ── Glass / Overlay helpers ────────────────────────────────
val VibeGlassWhite            = Color(0x0DFFFFFF)   // 5% white
val VibeGlassBorder           = Color(0x1AFFFFFF)   // 10% white
val VibeHoverHighlight        = Color(0x1AFFFFFF)   // 10% white (track row hover)
val VibeCardBackground        = Color(0xFF282828)   // Stitch media card spec
